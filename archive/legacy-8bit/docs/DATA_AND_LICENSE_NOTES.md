# Data And License Notes

The source code is MIT-licensed. See `LICENSE`.

The project display name is `ε-bit-ai-composer`. The Python package name remains ASCII-compatible as `ebit`, and the package metadata uses `epsilon-bit-ai-composer`.

## Generated Audio

Generated output is ignored:

```text
output/
*.wav
```

Selected MP3 examples are checked in under `examples/` so collaborators can listen quickly without regenerating everything.

## Reference MIDI

Some kept examples include community or third-party MIDI references. Their licensing may differ from this repository's source license.

Before broad public promotion:

- confirm the license or permission for each reference MIDI,
- remove any unclear third-party MIDI if needed,
- prefer links and analysis summaries over redistributing copyrighted transcriptions.

## AI-Written Material

Many earlier analysis notes were AI-written and were removed from the public tree. The remaining docs summarize the project state, but human listening remains the real quality gate.
